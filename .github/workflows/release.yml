name: "Release"

on:
  workflow_dispatch:
    inputs:
      bump:
        description: "Version bump type"
        required: true
        default: "patch"
        type: choice
        options:
          - patch
          - minor
          - major

# Never run two releases against the same ref at once; queue instead of cancel
# so an in-flight publish is never killed mid-upload.
concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  prepare-release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    outputs:
      tag_name: ${{ steps.bump.outputs.version }}
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
          token: ${{ secrets.GH_RELEASER_TOKEN }}

      - name: install Node.js
        uses: actions/setup-node@v7
        with:
          node-version: "20"

      - name: install bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: install Rust stable
        uses: dtolnay/rust-toolchain@stable

      # Cargo registry index/cache only — no build artifacts here, so the full
      # rust-cache action would be wasted weight. This just speeds `cargo update`.
      - name: cache cargo registry
        uses: actions/cache@v6
        with:
          path: |
            ~/.cargo/registry/index
            ~/.cargo/registry/cache
          key: ${{ runner.os }}-cargo-registry-${{ hashFiles('src-tauri/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-registry-

      - name: Bump versions, commit, and tag
        id: bump
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          # 1. Bump package.json (updates the version without tagging)
          npm version ${{ github.event.inputs.bump }} --no-git-tag-version
          NEW_VERSION=$(node -p "require('./package.json').version")

          # 2. Bump tauri.conf.json
          tmp=$(mktemp)
          jq ".version = \"$NEW_VERSION\"" src-tauri/tauri.conf.json > "$tmp"
          mv "$tmp" src-tauri/tauri.conf.json

          # 3. Bump Cargo.toml
          sed -i "0,/^version = \".*\"/s/^version = \".*\"/version = \"$NEW_VERSION\"/" src-tauri/Cargo.toml

          # 4. Refresh lockfiles. `cargo update -p` only rewrites the workspace
          #    member's entry in Cargo.lock — no compile, no system GTK deps.
          bun install
          (cd src-tauri && cargo update -p packetsniffer)

          # 5. Commit and tag
          git add package.json bun.lock src-tauri/tauri.conf.json src-tauri/Cargo.toml src-tauri/Cargo.lock
          git commit -m "chore: release v$NEW_VERSION"
          git tag "v$NEW_VERSION"

          # 6. Push commit and tag
          git push origin HEAD
          git push origin "v$NEW_VERSION"

          # Export the tag for the next job
          echo "version=v$NEW_VERSION" >> $GITHUB_OUTPUT

  publish-tauri:
    needs: prepare-release
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: "macos-latest"
            args: "--target aarch64-apple-darwin"
          - platform: "macos-latest"
            args: "--target x86_64-apple-darwin"
          - platform: "ubuntu-22.04"
            args: ""
          - platform: "windows-latest"
            args: ""

    runs-on: ${{ matrix.platform }}
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ needs.prepare-release.outputs.tag_name }}

      - name: install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.platform == 'macos-latest' && 'aarch64-apple-darwin,x86_64-apple-darwin' || '' }}

      - name: rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: "src-tauri -> target"
          # Both macOS targets run under the same job_id; without this suffix
          # they'd clobber each other's cache. Keep one cache slot per target.
          key: ${{ matrix.platform }}-${{ matrix.args }}
          cache-on-failure: true

      - name: install dependencies (ubuntu only)
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y libgtk-3-dev libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf

      - name: install bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: cache bun dependencies
        uses: actions/cache@v6
        with:
          path: ~/.bun/install/cache
          key: ${{ runner.os }}-bun-${{ hashFiles('bun.lock') }}
          restore-keys: ${{ runner.os }}-bun-

      - name: install frontend dependencies
        run: bun install --frozen-lockfile

      - uses: tauri-apps/tauri-action@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
        with:
          tagName: ${{ needs.prepare-release.outputs.tag_name }}
          releaseName: "PacketSniffer ${{ needs.prepare-release.outputs.tag_name }}"
          releaseBody: |
            > [!CAUTION]
            > This is a very early stage of the application. Things are subject to change and expect bugs. NOT tested outside of Windows

            **Full Changelog**: https://github.com/WarFiN123/PacketSniffer/commits/${{ needs.prepare-release.outputs.tag_name }}
          releaseDraft: false
          prerelease: false
          args: ${{ matrix.args }}

      # Remove signature files from the release
      - name: Remove signature files from release
        if: success()
        uses: actions/github-script@v9
        with:
          script: |
            const owner = context.repo.owner;
            const repo = context.repo.repo;
            const tag = '${{ needs.prepare-release.outputs.tag_name }}';

            const release = await github.rest.repos.getReleaseByTag({
              owner,
              repo,
              tag
            });

            for (const asset of release.data.assets) {
              if (asset.name.endsWith('.pub') || asset.name.endsWith('.sig')) {
                console.log(`Deleting ${asset.name}...`);
                await github.rest.repos.deleteReleaseAsset({
                  owner,
                  repo,
                  asset_id: asset.id
                });
              }
            }
